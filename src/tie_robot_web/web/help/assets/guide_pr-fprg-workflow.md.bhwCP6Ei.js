import { _ as _imports_0$1 } from "./chunks/pr-fprg-result.DYZDXurg.js";
import { _ as _export_sfc, o as openBlock, c as createElementBlock, ae as createStaticVNode } from "./chunks/framework.BxzTKQzU.js";
const _imports_0 = "/help/images/visual/pr-fprg-workflow.png";
const _imports_2 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-01_input_workspace.png";
const _imports_3 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-02_rectified_ir.png";
const _imports_4 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-03_rectified_depth.png";
const _imports_5 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-04_depth_response.png";
const _imports_6 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-05_vertical_profile.png";
const _imports_7 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-06_horizontal_profile.png";
const _imports_8 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-07_initial_frequency_phase_grid.png";
const _imports_9 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-08_peak_supported_lines.png";
const _imports_10 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-09_continuous_line_check.png";
const _imports_11 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-10_spacing_pruned_lines.png";
const _imports_12 = "/help/images/visual/pr-fprg-steps/pr-fprg-step-11_final_result.png";
const __pageData = JSON.parse('{"title":"PR-FPRG 识别流程详解","description":"","frontmatter":{},"headers":[],"relativePath":"guide/pr-fprg-workflow.md","filePath":"guide/pr-fprg-workflow.md"}');
const _sfc_main = { name: "guide/pr-fprg-workflow.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="pr-fprg-识别流程详解" tabindex="-1">PR-FPRG 识别流程详解 <a class="header-anchor" href="#pr-fprg-识别流程详解" aria-label="Permalink to &quot;PR-FPRG 识别流程详解&quot;">​</a></h1><p>本页说明当前绑扎点识别链路如何从相机图像一步步变成 12 个绑扎点。核心口径是：</p><ul><li>不再按整张工作区铺满网格。</li><li>不再假设钢筋一定与画面水平/垂直；钢筋在地面上本身斜摆时，按钢筋真实方向估计线族。</li><li>候选线必须同时满足「一维峰值」和「二维连续钢筋条」。</li><li>13、14、15 这类底部垫高件或起子造成的伪点，优先在「二维连续钢筋条验证」删除。</li><li>「主间距一致性裁剪」保留为最后兜底，不作为起子伪线的主要过滤层。</li><li>红外图像用于显示和人工核对；正式运行优先使用 <code>raw_world_coord</code> 深度响应，响应图梯度只作为 <code>theta</code> 候选方向先验，最终线仍要通过 PR-FPRG 峰值与二维连续 ridge 验证。</li><li>旧 <code>RANSAC + Hough + pre_img</code> pointAI 识别链路已经归档，不参与当前运行主链。</li></ul><p>目标版本流水线是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>透视展开工作区</span></span>\n<span class="line"><span>-&gt; 深度响应图</span></span>\n<span class="line"><span>-&gt; 自动估计两组钢筋真实方向 theta1 / theta2</span></span>\n<span class="line"><span>-&gt; 沿各自法线方向生成 profile</span></span>\n<span class="line"><span>-&gt; 在 rho 轴上找周期峰值</span></span>\n<span class="line"><span>-&gt; 每条钢筋线表示为 (theta, rho)</span></span>\n<span class="line"><span>-&gt; 连续钢筋条验证沿 theta 方向采样</span></span>\n<span class="line"><span>-&gt; ridge 判断沿法线方向检查「中间强、两侧弱」</span></span>\n<span class="line"><span>-&gt; 两组斜线求交点</span></span>\n<span class="line"><span>-&gt; 逆透视投回原图</span></span></code></pre></div><h2 id="总流程图" tabindex="-1">总流程图 <a class="header-anchor" href="#总流程图" aria-label="Permalink to &quot;总流程图&quot;">​</a></h2><p><img src="' + _imports_0 + '" alt="PR-FPRG 总流程"></p><p>这张图对应当前工程链路：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/web/pointAI/run_workspace_s2</span></span>\n<span class="line"><span>-&gt; pointAI.manual_workspace_s2_callback()</span></span>\n<span class="line"><span>-&gt; run_manual_workspace_s2_pipeline(publish=true)</span></span>\n<span class="line"><span>-&gt; /pointAI/manual_workspace_s2_result_raw</span></span>\n<span class="line"><span>-&gt; /pointAI/manual_workspace_s2_points</span></span>\n<span class="line"><span>-&gt; /coordinate_point</span></span>\n<span class="line"><span>-&gt; /tf pr_fprg_bind_point_*</span></span></code></pre></div><h2 id="当前帧最终结果" tabindex="-1">当前帧最终结果 <a class="header-anchor" href="#当前帧最终结果" aria-label="Permalink to &quot;当前帧最终结果&quot;">​</a></h2><p><img src="' + _imports_0$1 + '" alt="当前帧 PR-FPRG 最终结果"></p><p>这张图由当前相机现场帧重新生成，来源是正式同步的 <code>/Scepter/worldCoord/raw_world_coord</code>、<code>/Scepter/worldCoord/world_coord</code> 和 <code>/Scepter/ir/image_raw</code>，不是旧轴向截图。当前帧识别结果为：</p><ul><li>响应来源：<code>depth_background_minus_filled</code></li><li>第一组线族角度：<code>88°</code>，<code>rho = [-262, -172, -84]</code></li><li>第二组线族角度：<code>178°</code>，<code>rho = [-297, -206, -121, -14]</code></li><li>绑扎点数量：<code>12</code></li></ul><p>当前帧里钢筋方向接近 rectified 平面的横/竖方向，所以结果看起来接近水平/垂直；如果钢筋在地面上本身斜摆，<code>line_angle_deg</code> 会随钢筋真实方向变化，逆透视回原图后也会沿着钢筋走向画线。</p><h2 id="每一步图像处理" tabindex="-1">每一步图像处理 <a class="header-anchor" href="#每一步图像处理" aria-label="Permalink to &quot;每一步图像处理&quot;">​</a></h2><h3 id="_1-同步当前帧与工作区" tabindex="-1">1. 同步当前帧与工作区 <a class="header-anchor" href="#_1-同步当前帧与工作区" aria-label="Permalink to &quot;1. 同步当前帧与工作区&quot;">​</a></h3><p><img src="' + _imports_2 + '" alt="1. 同步当前帧与工作区"></p><p>输入来自 3 个地方：</p><ul><li><code>/Scepter/ir/image_raw</code>：用于显示、叠加和人工检查。</li><li><code>/Scepter/worldCoord/raw_world_coord</code>：保存每个像素对应的相机系 <code>x/y/z</code>。</li><li><code>manual_workspace_quad.json</code>：保存手动工作区四边形像素点。</li></ul><p>这一步只确定「在哪块区域里识别」，不做绑扎点判断。</p><h3 id="_2-透视展开工作区" tabindex="-1">2. 透视展开工作区 <a class="header-anchor" href="#_2-透视展开工作区" aria-label="Permalink to &quot;2. 透视展开工作区&quot;">​</a></h3><p><img src="' + _imports_3 + '" alt="2. 透视展开后的 IR 工作区"></p><p>把原图中的四边形工作区映射成规则矩形：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>source_points = 原图四边形角点</span></span>\n<span class="line"><span>destination_points = rectified 矩形角点</span></span>\n<span class="line"><span>forward_h = cv2.getPerspectiveTransform(source_points, destination_points)</span></span>\n<span class="line"><span>inverse_h = cv2.getPerspectiveTransform(destination_points, source_points)</span></span></code></pre></div><p>后续周期、线和交点都先在 rectified 平面里处理。透视展开负责消除相机视角造成的投影变形；如果钢筋在地面上本身就是斜摆，后续的方向自适应线族会按真实钢筋方向继续识别。</p><h3 id="_3-展开深度并填补无效值" tabindex="-1">3. 展开深度并填补无效值 <a class="header-anchor" href="#_3-展开深度并填补无效值" aria-label="Permalink to &quot;3. 展开深度并填补无效值&quot;">​</a></h3><p><img src="' + _imports_4 + '" alt="3. 展开并填补后的深度"></p><p>从 <code>raw_world_coord[:, :, 2]</code> 取原始深度 <code>z</code>，同时生成有效深度 mask。透视展开后，如果某些像素无效，会用当前有效区域深度中位数填补。</p><p>这一步的目的不是直接找线，而是把深度图整理成可做背景差分的规则矩形图。</p><h3 id="_4-生成钢筋凸起响应图" tabindex="-1">4. 生成钢筋凸起响应图 <a class="header-anchor" href="#_4-生成钢筋凸起响应图" aria-label="Permalink to &quot;4. 生成钢筋凸起响应图&quot;">​</a></h3><p><img src="' + _imports_5 + '" alt="4. 深度背景差响应图"></p><p>当前主要响应来自深度背景差：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>background_depth = GaussianBlur(filled_depth)</span></span>\n<span class="line"><span>response = background_depth - filled_depth</span></span>\n<span class="line"><span>response = normalize(response, valid_mask)</span></span></code></pre></div><p>直观理解：钢筋条相对板面有高度变化，经过背景平滑后会在响应图中形成线状亮带。后面的峰值、连续线和间距裁剪都基于这张响应图。</p><h3 id="_5-生成方向-theta-rho-profile" tabindex="-1">5. 生成方向 <code>theta/rho</code> profile <a class="header-anchor" href="#_5-生成方向-theta-rho-profile" aria-label="Permalink to &quot;5. 生成方向 `theta/rho` profile&quot;">​</a></h3><p><img src="' + _imports_6 + '" alt="5. 第一组 theta/rho profile"></p><p><img src="' + _imports_7 + '" alt="6. 第二组 theta/rho profile"></p><p>当前主链会扫描候选钢筋方向 <code>theta</code>，把二维响应图沿该方向的法向 <code>rho</code> 压缩成一维 profile：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>normal(theta) = [-sin(theta), cos(theta)]</span></span>\n<span class="line"><span>rho = normal_x * x + normal_y * y</span></span>\n<span class="line"><span>profile_theta[rho] = response 在同一 rho bin 内的平均值</span></span></code></pre></div><p>profile 的峰值表示「这条 <code>rho</code> 线整体更像钢筋条」。当前版本还会从响应图梯度里提取方向先验，把真实钢筋方向及其正交方向放进候选池；这不是用 Hough 出点，最终线仍由 PR-FPRG 的峰值和连续 ridge 决定。</p><p>这样钢筋网只要在 rectified 平面内仍是两组近似平行线，就可以识别；它不要求线条刚好平行于图像 X/Y 轴。但单独看 profile 不够，因为局部杂物也可能把某条 <code>rho</code> 线抬成峰值，所以后面还要做二维连续性验证。</p><h3 id="_6-频相回归生成候选网格" tabindex="-1">6. 频相回归生成候选网格 <a class="header-anchor" href="#_6-频相回归生成候选网格" aria-label="Permalink to &quot;6. 频相回归生成候选网格&quot;">​</a></h3><p><img src="' + _imports_8 + '" alt="7. 初始频相候选线"></p><p>对每个方向的 profile 做平滑和中心化后，遍历候选周期并计算自相关分数，得到主周期；再遍历相位，取平均响应最高的相位。候选方向先经过响应图方向先验和正交补全，再从候选池里选择两组近似正交、连续性更好的线族：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>theta_family = argmax score(profile_theta)</span></span>\n<span class="line"><span>period = argmax corr(profile_theta[t], profile_theta[t + period])</span></span>\n<span class="line"><span>phase = argmax mean(profile_theta[phase::period])</span></span>\n<span class="line"><span>line_rhos = rho_min + phase + k * period</span></span></code></pre></div><p>这一步只生成「可能的」周期候选线，还不是最终绑扎点。</p><h3 id="_7-局部峰值修正与峰值支撑筛选" tabindex="-1">7. 局部峰值修正与峰值支撑筛选 <a class="header-anchor" href="#_7-局部峰值修正与峰值支撑筛选" aria-label="Permalink to &quot;7. 局部峰值修正与峰值支撑筛选&quot;">​</a></h3><p><img src="' + _imports_9 + '" alt="8. 峰值支撑后的候选线"></p><p>候选 <code>rho</code> 线会在邻域内吸附到局部响应峰，然后做峰值支撑筛选：</p><ul><li>候选线附近必须有足够强的一维 profile 峰值。</li><li>太弱的周期线会被删掉。</li><li>同一根钢筋附近重复候选会做非极大值抑制。</li></ul><p>这一步解决「不要整张图按周期铺满」的问题，但还不能完全删除底部伪线，因为底部垫高件也可能有强峰值。</p><h3 id="_8-二维连续钢筋条验证" tabindex="-1">8. 二维连续钢筋条验证 <a class="header-anchor" href="#_8-二维连续钢筋条验证" aria-label="Permalink to &quot;8. 二维连续钢筋条验证&quot;">​</a></h3><p><img src="' + _imports_10 + '" alt="9. 二维连续钢筋条验证"></p><p>对每条候选线，在二维响应图上沿线方向采样，并把线长切成多个段。同时检查每个采样点的横截面是不是「中间强、两侧弱」且贴近候选线中心的 ridge，而不是贴边的一整片宽亮带：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for segment in line_segments:</span></span>\n<span class="line"><span>    ridge_like = 强响应 and 中心 ridge 两侧有落差 and ridge 偏移不能离候选线太远</span></span>\n<span class="line"><span>    strong_fraction = ridge_like 采样数 / 该段有效采样数</span></span>\n<span class="line"><span>line_supported = 支撑段比例 &gt;= min_segment_coverage</span></span></code></pre></div><p>真实钢筋条应该在多数段上都有线状响应，并且横截面像一根窄钢筋。只有局部亮块、局部阴影、起子、垫高件或斜穿过交叉点的伪线会在这里被删除。</p><p>当前帧中，峰值阶段第一组曾多出一条靠边候选线：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>peak_supported.family_0 = [-262, -172, -84, 12]</span></span>\n<span class="line"><span>continuous.family_0 = [-262.7, -172.0, -83.7]</span></span></code></pre></div><p>也就是说，靠边伪线没有等到距离筛选，而是在连续钢筋条验证阶段已经被删掉。</p><h3 id="_9-主间距一致性裁剪" tabindex="-1">9. 主间距一致性裁剪 <a class="header-anchor" href="#_9-主间距一致性裁剪" aria-label="Permalink to &quot;9. 主间距一致性裁剪&quot;">​</a></h3><p><img src="' + _imports_11 + '" alt="10. 主间距一致性裁剪"></p><p>绿色线是保留线。如果连续验证之后仍有靠边伪线，距离筛选会把不符合主网格间距的线标红删除；当前帧里的靠边伪线已经在上一步删除，所以这里没有额外红线。</p><p>当前裁剪逻辑是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>diffs = 相邻线间距</span></span>\n<span class="line"><span>reference_spacing = 较大半区间距的中位数</span></span>\n<span class="line"><span>if gap &lt; reference_spacing * 0.65:</span></span>\n<span class="line"><span>    删除更像边缘伪线的一条</span></span></code></pre></div><p>因此，距离筛选现在作为兜底保险：正常情况下起子伪线先由二维连续 ridge 验证删除；若有残留，再由主间距一致性删除。最终只保留真实钢筋线族。</p><h3 id="_10-两组线族求交与逆透视映射" tabindex="-1">10. 两组线族求交与逆透视映射 <a class="header-anchor" href="#_10-两组线族求交与逆透视映射" aria-label="Permalink to &quot;10. 两组线族求交与逆透视映射&quot;">​</a></h3><p><img src="' + _imports_12 + '" alt="11. 两组线族求交后的最终结果"></p><p>最终保留的两组线族使用线方程求交：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>normal_a . point = rho_a</span></span>\n<span class="line"><span>normal_b . point = rho_b</span></span>\n<span class="line"><span>rectified_intersection = solve([normal_a; normal_b], [rho_a, rho_b])</span></span></code></pre></div><p>再用 <code>inverse_h</code> 投回原始 IR 图像像素：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>image_points = cv2.perspectiveTransform(rectified_intersections, inverse_h)</span></span></code></pre></div><p>这就是结果图上看到的黄色编号点。</p><h3 id="_11-从原始世界坐标取点" tabindex="-1">11. 从原始世界坐标取点 <a class="header-anchor" href="#_11-从原始世界坐标取点" aria-label="Permalink to &quot;11. 从原始世界坐标取点&quot;">​</a></h3><p>每个像素交点会去 <code>/Scepter/worldCoord/raw_world_coord</code> 中取相机系三维坐标：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>camera_xyz = raw_world_coord[pixel_y, pixel_x, :3]</span></span></code></pre></div><p>如果该像素无效，会在小邻域内找最近有效值。当前 pointAI 发布的点保持相机坐标系语义，前端 3D Scene 再通过 TF 投到全局显示层。</p><h2 id="代码落点" tabindex="-1">代码落点 <a class="header-anchor" href="#代码落点" aria-label="Permalink to &quot;代码落点&quot;">​</a></h2><ul><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/manual_workspace_s2.py</code>：运行时主链，负责准备输入、调用算法、发布结果。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/perception/workspace_s2.py</code>：方向线族估计、周期估计、峰值筛选、连续线验证、间距裁剪和线族求交。</li><li><code>src/tie_robot_perception/tools/pr_fprg_peak_supported_probe.py</code>：独立探针，可抓取当前帧并导出本页使用的逐步图像。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/rendering.py</code>：把最终线和点渲染到结果图。</li></ul><h2 id="独立复现命令" tabindex="-1">独立复现命令 <a class="header-anchor" href="#独立复现命令" aria-label="Permalink to &quot;独立复现命令&quot;">​</a></h2><p>启动相机链后运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/ros/noetic/setup.bash</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> devel/setup.bash</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/tie_robot_perception/tools/pr_fprg_peak_supported_probe.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --timeout</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --docs-assets-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src/tie_robot_web/help/public/images/visual/pr-fprg-steps</span></span></code></pre></div><p>输出内容：</p><ul><li><code>.debug_frames/pr_fprg_peak_supported_probe_*/summary.json</code></li><li><code>.debug_frames/pr_fprg_peak_supported_probe_*/steps/*.png</code></li><li><code>src/tie_robot_web/help/public/images/visual/pr-fprg-steps/*.png</code></li></ul>', 83)
  ])]);
}
const prFprgWorkflow = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  prFprgWorkflow as default
};
