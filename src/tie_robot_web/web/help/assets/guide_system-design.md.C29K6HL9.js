import { _ as _export_sfc, o as openBlock, c as createElementBlock, ae as createStaticVNode } from "./chunks/framework.BxzTKQzU.js";
const _imports_0 = "/help/images/architecture/tie-robot-system-architecture.svg";
const __pageData = JSON.parse('{"title":"工程设计与架构图","description":"","frontmatter":{},"headers":[],"relativePath":"guide/system-design.md","filePath":"guide/system-design.md"}');
const _sfc_main = { name: "guide/system-design.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="工程设计与架构图" tabindex="-1">工程设计与架构图 <a class="header-anchor" href="#工程设计与架构图" aria-label="Permalink to &quot;工程设计与架构图&quot;">​</a></h1><p>这页记录当前工程的设计边界和主架构图。它不是旧包名的历史图，而是按当前 <code>tie_robot_*</code> 八个主包、独立驱动守护、常驻 <code>rosbridge_stack.launch</code> 和算法后端重新整理的版本。</p><p><img src="' + _imports_0 + '" alt="simple_lashingrobot_ws 工程设计总图"></p><p><a href="/help/images/architecture/tie-robot-system-architecture.png">打开 PNG 版本</a></p><h2 id="核心设计口径" tabindex="-1">核心设计口径 <a class="header-anchor" href="#核心设计口径" aria-label="Permalink to &quot;核心设计口径&quot;">​</a></h2><ul><li><code>tie_robot_msgs</code> 只放全局 <code>msg / srv / action</code>，避免接口散落在业务包里。</li><li><code>tie_robot_hw</code> 是硬件与协议层，只提供驱动层原子动作：索驱 TCP 帧、线性模组 Modbus 寄存器动作、相机 SDK 取帧。</li><li><code>tie_robot_perception</code> 负责相机世界点处理、PR-FPRG 视觉识别和 TF 相关感知节点。</li><li><code>tie_robot_control</code> 负责编排线性模组运动控制、到位等待、FINISHALL 等末端动作链。</li><li><code>tie_robot_process</code> 负责编排扫描、规划、账本、索驱区域位姿和整条绑扎执行链。</li><li><code>tie_robot_web</code> 负责浏览器前端、ROS bridge 接入、系统控制 HTTP 接口和帮助站。</li><li><code>tie_robot_bringup</code> 只做 launch 装配和 systemd 安装脚本。</li></ul><h2 id="驱动层原子动作" tabindex="-1">驱动层原子动作 <a class="header-anchor" href="#驱动层原子动作" aria-label="Permalink to &quot;驱动层原子动作&quot;">​</a></h2><p>索驱和线性模组都遵循同一个边界：驱动层只表达硬件能直接理解的一步动作，执行链调度放在控制/算法层。</p><ul><li>索驱驱动：<code>moveToPose()</code>、<code>sendStop()</code> 等 TCP 原子指令。</li><li>线性模组驱动：<code>writeQueuedPoints()</code>、<code>pulseExecutionEnable()</code>、<code>setZeroRequest()</code> 等寄存器原子动作。</li><li>控制/算法层：决定何时清 FINISHALL、何时写点位、何时触发 <code>EN_DISABLE 0-&gt;1</code>、何时等待到位或处理暂停。</li></ul><p>这样做的目标是让硬件协议不掺业务状态机，后续排查“按钮下发了但没动”“暂停回报慢”“触发位没起边沿”时，可以快速定位是在驱动动作、控制编排还是硬件反馈。</p><h2 id="运行分层" tabindex="-1">运行分层 <a class="header-anchor" href="#运行分层" aria-label="Permalink to &quot;运行分层&quot;">​</a></h2><h3 id="常驻接入层" tabindex="-1">常驻接入层 <a class="header-anchor" href="#常驻接入层" aria-label="Permalink to &quot;常驻接入层&quot;">​</a></h3><p><code>tie-robot-rosbridge.service</code> 拉起 <code>rosbridge_stack.launch</code>，里面常驻：</p><ul><li><code>rosbridge_websocket</code> 和 <code>rosapi</code></li><li><code>tf2_web_republisher</code></li><li><code>robot_tf_broadcaster</code></li><li><code>gripper_tf_broadcaster</code></li><li><code>web_action_bridge_node</code></li><li><code>system_log_mux</code></li></ul><p>它们不跟随后端算法栈重启，浏览器页面、TF 回传、Web Action 桥接和系统日志面板可以保持在线。</p><h3 id="独立驱动守护" tabindex="-1">独立驱动守护 <a class="header-anchor" href="#独立驱动守护" aria-label="Permalink to &quot;独立驱动守护&quot;">​</a></h3><p>三个驱动由 systemd 分别守护：</p><ul><li><code>tie-robot-driver-suoqu.service</code> -&gt; <code>driver_suoqu.launch</code></li><li><code>tie-robot-driver-moduan.service</code> -&gt; <code>driver_moduan.launch</code></li><li><code>tie-robot-driver-camera.service</code> -&gt; <code>driver_camera.launch</code></li></ul><p>驱动之间互不拖拽；只测线性模组、只测相机或只测索驱时，不需要启动整套算法后端。</p><h3 id="算法后端" tabindex="-1">算法后端 <a class="header-anchor" href="#算法后端" aria-label="Permalink to &quot;算法后端&quot;">​</a></h3><p><code>run.launch</code> 只包含 <code>algorithm_stack.launch</code>，主节点包括：</p><ul><li><code>pointAINode</code></li><li><code>bind_map_builder</code></li><li><code>global_bind_planner</code></li><li><code>cabin_motion_controller</code></li><li><code>moduan_motion_controller</code></li><li><code>bind_task_executor</code></li></ul><p>后端可以由前端按钮通过受限 systemd 接口启动、停止或重启。后端未启动时，前端 Action 会明确失败，不再伪装成“有 rosbridge 发布但没有服务端订阅”。</p><h2 id="主数据闭环" tabindex="-1">主数据闭环 <a class="header-anchor" href="#主数据闭环" aria-label="Permalink to &quot;主数据闭环&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>浏览器前端</span></span>\n<span class="line"><span>-&gt; rosbridge_websocket</span></span>\n<span class="line"><span>-&gt; web_action_bridge_node / 前端直连话题</span></span>\n<span class="line"><span>-&gt; bind_task_executor / pointAINode</span></span>\n<span class="line"><span>-&gt; cabin_motion_controller / moduan_motion_controller</span></span>\n<span class="line"><span>-&gt; suoqu_driver_node / moduan_driver_node</span></span>\n<span class="line"><span>-&gt; 真实硬件</span></span></code></pre></div><p>视觉侧的数据闭环：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scepter 相机</span></span>\n<span class="line"><span>-&gt; scepter_manager</span></span>\n<span class="line"><span>-&gt; scepter_world_coord_processor</span></span>\n<span class="line"><span>-&gt; pointAINode(PR-FPRG)</span></span>\n<span class="line"><span>-&gt; /coordinate_point + /pointAI/* result</span></span>\n<span class="line"><span>-&gt; bind_task_executor + 前端 3D Scene</span></span></code></pre></div><p>执行侧的数据闭环：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bind_task_executor</span></span>\n<span class="line"><span>-&gt; pseudo_slam_points.json</span></span>\n<span class="line"><span>-&gt; pseudo_slam_bind_path.json</span></span>\n<span class="line"><span>-&gt; 索驱 area.cabin_pose</span></span>\n<span class="line"><span>-&gt; 线性模组 group.points[].x/y/z</span></span>\n<span class="line"><span>-&gt; bind_execution_memory.json</span></span></code></pre></div><h2 id="图源" tabindex="-1">图源 <a class="header-anchor" href="#图源" aria-label="Permalink to &quot;图源&quot;">​</a></h2><p>图源文件随帮助站一起维护：</p><ul><li><code>/images/architecture/tie-robot-system-architecture.dot</code></li><li><code>/images/architecture/tie-robot-system-architecture.svg</code></li><li><code>/images/architecture/tie-robot-system-architecture.png</code></li></ul><p>修改架构边界或 launch 拓扑后，请同步更新本页和图资产。</p>', 33)
  ])]);
}
const systemDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  systemDesign as default
};
