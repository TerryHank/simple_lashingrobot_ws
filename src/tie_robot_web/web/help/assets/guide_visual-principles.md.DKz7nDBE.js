import { _ as _export_sfc, o as openBlock, c as createElementBlock, ae as createStaticVNode } from "./chunks/framework.BxzTKQzU.js";
const _imports_0 = "/help/images/visual/pr-fprg-result.png";
const __pageData = JSON.parse('{"title":"视觉原理","description":"","frontmatter":{},"headers":[],"relativePath":"guide/visual-principles.md","filePath":"guide/visual-principles.md"}');
const _sfc_main = { name: "guide/visual-principles.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="视觉原理" tabindex="-1">视觉原理 <a class="header-anchor" href="#视觉原理" aria-label="Permalink to &quot;视觉原理&quot;">​</a></h1><h2 id="当前工作区-s2-方案" tabindex="-1">当前工作区 S2 方案 <a class="header-anchor" href="#当前工作区-s2-方案" aria-label="Permalink to &quot;当前工作区 S2 方案&quot;">​</a></h2><p>当前帮助站、前端选点页、运行时扫描识别里提到的手动工作区 <code>S2</code>，统一指：</p><ul><li><code>PR-FPRG 透视展开频相回归网格方案</code></li><li><code>PR-FPRG = Projective-Rectified Frequency-Phase Regression Grid</code></li></ul><p>这是当前认可的工作区识别主方案。</p><h2 id="当前方案效果图" tabindex="-1">当前方案效果图 <a class="header-anchor" href="#当前方案效果图" aria-label="Permalink to &quot;当前方案效果图&quot;">​</a></h2><p>下图是当前 <code>PR-FPRG</code> 在现场运行时的一次识别结果。图中的网格线和交点，不是在原图里直接硬画出来的，而是：</p><ul><li>先对工作区做透视展开</li><li>在 rectified 工作区里做频域周期检测与相位回归</li><li>再通过 inverse projective mapping 投回原图</li></ul><p><img src="' + _imports_0 + '" alt="PR-FPRG 现场结果图"></p><p>这张图对应的现场结果目录是：</p><ul><li><code>/.debug_frames/manual_workspace_s2_runtime_projective_fix/result.png</code></li></ul><p>对应结果摘要里，这次现场识别结果为：</p><ul><li><code>point_count = 272</code></li></ul><h2 id="当前方案效果图-1" tabindex="-1">当前方案效果图 <a class="header-anchor" href="#当前方案效果图-1" aria-label="Permalink to &quot;当前方案效果图&quot;">​</a></h2><p>下图是当前 <code>PR-FPRG</code> 在现场运行时的一次识别结果。图中的网格线和交点，不是在原图里直接硬画出来的，而是：</p><ul><li>先对工作区做透视展开</li><li>在 rectified 工作区里做频域周期检测与相位回归</li><li>再通过 inverse projective mapping 投回原图</li></ul><p><img src="' + _imports_0 + '" alt="PR-FPRG 现场结果图"></p><p>这张图对应的现场结果目录是：</p><ul><li><code>/.debug_frames/manual_workspace_s2_runtime_projective_fix/result.png</code></li></ul><p>对应结果摘要里，这次现场识别结果为：</p><ul><li><code>point_count = 272</code></li></ul><h2 id="为什么不是原图直接画网格" tabindex="-1">为什么不是原图直接画网格 <a class="header-anchor" href="#为什么不是原图直接画网格" aria-label="Permalink to &quot;为什么不是原图直接画网格&quot;">​</a></h2><p>钢筋工作区在原始 IR 图像里往往是梯形或任意四边形，如果直接在原图里：</p><ul><li>取工作区 bbox</li><li>做周期估计</li><li>直接画横线和竖线</li></ul><p>那么透视畸变会把真实钢筋间距拉坏，结果通常会表现为：</p><ul><li>主周期漂移</li><li>线与真实钢筋位置偏差大</li><li>网格更像“图像格子”，不像“工作平面里的真实钢筋网格”</li></ul><p>所以当前方案先做几何校正，再做网格估计。</p><h2 id="当前方案主链" tabindex="-1">当前方案主链 <a class="header-anchor" href="#当前方案主链" aria-label="Permalink to &quot;当前方案主链&quot;">​</a></h2><h3 id="_1-手动选四边形工作区" tabindex="-1">1. 手动选四边形工作区 <a class="header-anchor" href="#_1-手动选四边形工作区" aria-label="Permalink to &quot;1. 手动选四边形工作区&quot;">​</a></h3><p>前端用户先在 IR 图像上点出 4 个角点，形成手动工作区四边形。</p><p>后端保存：</p><ul><li><code>corner_pixels</code></li><li><code>corner_world_cabin_frame</code></li></ul><p>其中：</p><ul><li><code>corner_pixels</code> 用来描述图像上的四边形</li><li><code>corner_world_cabin_frame</code> 用来描述该四边形在全局工作坐标里的真实几何尺度</li></ul><h3 id="_2-透视展开" tabindex="-1">2. 透视展开 <a class="header-anchor" href="#_2-透视展开" aria-label="Permalink to &quot;2. 透视展开&quot;">​</a></h3><p>识别时，先把这个四边形工作区展开成规则平面：</p><ul><li>优先使用 <code>corner_world_cabin_frame</code> 来确定 rectified 工作区的真实宽高</li><li>只有在世界角点不存在时，才退回到图像像素边长</li></ul><p>这样 rectified 图里的钢筋间距才更接近真实工作面上的周期。</p><h3 id="_3-频域周期检测-相位回归" tabindex="-1">3. 频域周期检测 + 相位回归 <a class="header-anchor" href="#_3-频域周期检测-相位回归" aria-label="Permalink to &quot;3. 频域周期检测 + 相位回归&quot;">​</a></h3><p>在 rectified 工作区里：</p><ul><li>分别计算纵向和横向的 profile</li><li>用 <code>FFT / 自相关</code> 估主周期</li><li>再做相位回归，反推出整张规则网格</li></ul><p>这一步是当前方案的核心，所以名称里会强调：</p><ul><li><code>Frequency</code></li><li><code>Phase Regression</code></li></ul><h3 id="_4-结合谷底-线响应稳住网格" tabindex="-1">4. 结合谷底/线响应稳住网格 <a class="header-anchor" href="#_4-结合谷底-线响应稳住网格" aria-label="Permalink to &quot;4. 结合谷底/线响应稳住网格&quot;">​</a></h3><p>钢筋在高度图或响应图里，通常表现为更稳定的线状谷底或线响应。<br> 当前方案在规则平面里估完周期后，会结合这些响应来稳定最终网格，而不是只靠纯几何周期硬铺满全图。</p><h3 id="_5-投回原图" tabindex="-1">5. 投回原图 <a class="header-anchor" href="#_5-投回原图" aria-label="Permalink to &quot;5. 投回原图&quot;">​</a></h3><p>最终显示在前端和调试图上的线与交点，不是直接拿 rectified 图结果完事，而是会：</p><ul><li>通过 inverse projective mapping</li><li>再投回到原始 IR 图像</li></ul><p>所以你在原图上看到的线，会跟着工作区四边形几何走，而不是死板的图像横竖线。</p><h2 id="从相机-sdk-平面拟合到-pre-img-门控的原理链" tabindex="-1">从相机 SDK 平面拟合到 <code>pre_img()</code> 门控的原理链 <a class="header-anchor" href="#从相机-sdk-平面拟合到-pre-img-门控的原理链" aria-label="Permalink to &quot;从相机 SDK 平面拟合到 `pre_img()` 门控的原理链&quot;">​</a></h2><p>当前系统不是“相机一出图就直接走同一条识别链”，而是先经过一层上游几何清理，再分流到不同视觉入口。</p><h3 id="_1-相机-sdk-上游先做平面拟合" tabindex="-1">1. 相机 SDK 上游先做平面拟合 <a class="header-anchor" href="#_1-相机-sdk-上游先做平面拟合" aria-label="Permalink to &quot;1. 相机 SDK 上游先做平面拟合&quot;">​</a></h3><p>当前上游世界点处理在：</p><ul><li><code>src/tie_robot_perception/src/perception/scepter_world_coord_processor.cpp</code></li></ul><p>这里会先把原始深度图转成三维点，再做一次基于 <code>RANSAC</code> 的平面拟合：</p><ul><li><code>SACMODEL_PLANE</code></li><li><code>SAC_RANSAC</code></li></ul><p>拟合出的主平面内点会被剔掉，剩下的非平面点再重投影回二维图，发布成：</p><ul><li><code>world_coord</code></li></ul><p>同时，未经平面剔除的原始世界点会保留并发布成：</p><ul><li><code>raw_world_coord</code></li></ul><p>可以把它理解成：</p><ul><li><code>raw_world_coord</code>：原始世界点</li><li><code>world_coord</code>：去掉主平面后的“残差世界点”</li></ul><h3 id="_2-pointai-同时持有两套世界点" tabindex="-1">2. <code>pointAI</code> 同时持有两套世界点 <a class="header-anchor" href="#_2-pointai-同时持有两套世界点" aria-label="Permalink to &quot;2. `pointAI` 同时持有两套世界点&quot;">​</a></h3><p>当前 <code>pointAI</code> 会同时使用：</p><ul><li>原始图像</li><li><code>raw_world_coord</code></li><li><code>world_coord</code></li></ul><p>其中：</p><ul><li><code>raw_world_coord</code> 更适合做手动工作区 <code>PR-FPRG</code></li><li><code>world_coord</code> / 深度残差链更接近旧的局部检测门控</li></ul><h3 id="_3-pre-img-是旧局部视觉链的门控入口" tabindex="-1">3. <code>pre_img()</code> 是旧局部视觉链的门控入口 <a class="header-anchor" href="#_3-pre-img-是旧局部视觉链的门控入口" aria-label="Permalink to &quot;3. `pre_img()` 是旧局部视觉链的门控入口&quot;">​</a></h3><p>当前旧的局部视觉识别主入口是：</p><ul><li><code>pre_img()</code></li></ul><p>它做的事情包括：</p><ul><li>取图像和世界点通道</li><li>应用检测遮挡</li><li>对深度残差做二值化、细化、霍夫线提取</li><li>交点聚类</li><li>回查世界坐标</li><li>最终筛成可下游使用的点集</li></ul><p>这条链本质上还是“局部矩阵 / 局部绑扎点门控”思路。</p><h3 id="_4-为什么扫描层不再先走-pre-img" tabindex="-1">4. 为什么扫描层不再先走 <code>pre_img()</code> <a class="header-anchor" href="#_4-为什么扫描层不再先走-pre-img" aria-label="Permalink to &quot;4. 为什么扫描层不再先走 `pre_img()`&quot;">​</a></h3><p>当前扫描层的 <code>scan_only</code> 已经被改成：</p><ul><li>先直接尝试 <code>PR-FPRG</code></li><li>不再要求先由 <code>pre_img()</code> 出点成功后，才允许继续</li></ul><p>原因是：</p><ul><li><code>PR-FPRG</code> 的目标是做整块手动工作区的规则网格恢复</li><li>它依赖的是“工作区透视展开 + 频域周期检测 + 相位回归”</li><li>不是旧的局部矩阵门控逻辑</li></ul><p>所以当前系统里：</p><ul><li>扫描层：优先走 <code>PR-FPRG</code></li><li>执行层局部微调：仍然可以继续走原来的局部门控视觉链</li></ul><h3 id="_5-这条链路的核心分工" tabindex="-1">5. 这条链路的核心分工 <a class="header-anchor" href="#_5-这条链路的核心分工" aria-label="Permalink to &quot;5. 这条链路的核心分工&quot;">​</a></h3><p>可以把当前视觉原理简单理解成：</p><ol><li>相机 SDK 上游先做主平面剔除，提供两套世界点</li><li>旧链 <code>pre_img()</code> 负责局部矩阵 / 局部门控</li><li>新链 <code>PR-FPRG</code> 负责手动工作区整块规则网格恢复</li><li>扫描层和执行层不要再混用同一条识别语义</li></ol><p>这也是为什么当前帮助站要把 <code>PR-FPRG</code> 单独命名，而不是继续把它混在旧 <code>pre_img()</code> 逻辑里讲。</p><h2 id="从相机-sdk-平面拟合到-pre-img-门控的原理链-1" tabindex="-1">从相机 SDK 平面拟合到 <code>pre_img()</code> 门控的原理链 <a class="header-anchor" href="#从相机-sdk-平面拟合到-pre-img-门控的原理链-1" aria-label="Permalink to &quot;从相机 SDK 平面拟合到 `pre_img()` 门控的原理链&quot;">​</a></h2><p>当前系统不是“相机一出图就直接走同一条识别链”，而是先经过一层上游几何清理，再分流到不同视觉入口。</p><h3 id="_1-相机-sdk-上游先做平面拟合-1" tabindex="-1">1. 相机 SDK 上游先做平面拟合 <a class="header-anchor" href="#_1-相机-sdk-上游先做平面拟合-1" aria-label="Permalink to &quot;1. 相机 SDK 上游先做平面拟合&quot;">​</a></h3><p>当前上游世界点处理在：</p><ul><li><code>src/tie_robot_perception/src/perception/scepter_world_coord_processor.cpp</code></li></ul><p>这里会先把原始深度图转成三维点，再做一次基于 <code>RANSAC</code> 的平面拟合：</p><ul><li><code>SACMODEL_PLANE</code></li><li><code>SAC_RANSAC</code></li></ul><p>拟合出的主平面内点会被剔掉，剩下的非平面点再重投影回二维图，发布成：</p><ul><li><code>world_coord</code></li></ul><p>同时，未经平面剔除的原始世界点会保留并发布成：</p><ul><li><code>raw_world_coord</code></li></ul><p>可以把它理解成：</p><ul><li><code>raw_world_coord</code>：原始世界点</li><li><code>world_coord</code>：去掉主平面后的“残差世界点”</li></ul><h3 id="_2-pointai-同时持有两套世界点-1" tabindex="-1">2. <code>pointAI</code> 同时持有两套世界点 <a class="header-anchor" href="#_2-pointai-同时持有两套世界点-1" aria-label="Permalink to &quot;2. `pointAI` 同时持有两套世界点&quot;">​</a></h3><p>当前 <code>pointAI</code> 会同时使用：</p><ul><li>原始图像</li><li><code>raw_world_coord</code></li><li><code>world_coord</code></li></ul><p>其中：</p><ul><li><code>raw_world_coord</code> 更适合做手动工作区 <code>PR-FPRG</code></li><li><code>world_coord</code> / 深度残差链更接近旧的局部检测门控</li></ul><h3 id="_3-pre-img-是旧局部视觉链的门控入口-1" tabindex="-1">3. <code>pre_img()</code> 是旧局部视觉链的门控入口 <a class="header-anchor" href="#_3-pre-img-是旧局部视觉链的门控入口-1" aria-label="Permalink to &quot;3. `pre_img()` 是旧局部视觉链的门控入口&quot;">​</a></h3><p>当前旧的局部视觉识别主入口是：</p><ul><li><code>pre_img()</code></li></ul><p>它做的事情包括：</p><ul><li>取图像和世界点通道</li><li>应用检测遮挡</li><li>对深度残差做二值化、细化、霍夫线提取</li><li>交点聚类</li><li>回查世界坐标</li><li>最终筛成可下游使用的点集</li></ul><p>这条链本质上还是“局部矩阵 / 局部绑扎点门控”思路。</p><h3 id="_4-为什么扫描层不再先走-pre-img-1" tabindex="-1">4. 为什么扫描层不再先走 <code>pre_img()</code> <a class="header-anchor" href="#_4-为什么扫描层不再先走-pre-img-1" aria-label="Permalink to &quot;4. 为什么扫描层不再先走 `pre_img()`&quot;">​</a></h3><p>当前扫描层的 <code>scan_only</code> 已经被改成：</p><ul><li>先直接尝试 <code>PR-FPRG</code></li><li>不再要求先由 <code>pre_img()</code> 出点成功后，才允许继续</li></ul><p>原因是：</p><ul><li><code>PR-FPRG</code> 的目标是做整块手动工作区的规则网格恢复</li><li>它依赖的是“工作区透视展开 + 频域周期检测 + 相位回归”</li><li>不是旧的局部矩阵门控逻辑</li></ul><p>所以当前系统里：</p><ul><li>扫描层：优先走 <code>PR-FPRG</code></li><li>执行层局部微调：仍然可以继续走原来的局部门控视觉链</li></ul><h3 id="_5-这条链路的核心分工-1" tabindex="-1">5. 这条链路的核心分工 <a class="header-anchor" href="#_5-这条链路的核心分工-1" aria-label="Permalink to &quot;5. 这条链路的核心分工&quot;">​</a></h3><p>可以把当前视觉原理简单理解成：</p><ol><li>相机 SDK 上游先做主平面剔除，提供两套世界点</li><li>旧链 <code>pre_img()</code> 负责局部矩阵 / 局部门控</li><li>新链 <code>PR-FPRG</code> 负责手动工作区整块规则网格恢复</li><li>扫描层和执行层不要再混用同一条识别语义</li></ol><p>这也是为什么当前帮助站要把 <code>PR-FPRG</code> 单独命名，而不是继续把它混在旧 <code>pre_img()</code> 逻辑里讲。</p><h2 id="当前方案的关键优势" tabindex="-1">当前方案的关键优势 <a class="header-anchor" href="#当前方案的关键优势" aria-label="Permalink to &quot;当前方案的关键优势&quot;">​</a></h2><ul><li>先消除工作区透视畸变，再做网格估计</li><li>周期估计更稳，不容易被原图形变带偏</li><li>结果更接近真实钢筋面，而不是图像格子</li><li>能利用世界角点，把工作区展开尺度拉回真实物理尺寸</li></ul><h2 id="当前方案明确不允许退回的旧做法" tabindex="-1">当前方案明确不允许退回的旧做法 <a class="header-anchor" href="#当前方案明确不允许退回的旧做法" aria-label="Permalink to &quot;当前方案明确不允许退回的旧做法&quot;">​</a></h2><p>以下做法都视为退化，不应再恢复：</p><ol><li>直接在工作区像素 bbox 里估周期</li><li>用图像像素边长直接决定 rectified 尺度</li><li>在原图里直接铺横竖线</li><li>只调参数，不修几何主链</li></ol><h2 id="方案落点" tabindex="-1">方案落点 <a class="header-anchor" href="#方案落点" aria-label="Permalink to &quot;方案落点&quot;">​</a></h2><p>当前关键实现位于：</p><ul><li><code>src/tie_robot_perception/scripts/pointAI.py</code></li><li><code>src/tie_robot_perception/src/tie_robot_perception/perception/workspace_s2.py</code></li></ul><p>更详细的交接知识条目见：</p><ul><li><code>docs/handoff/2026-04-23_pr_fprg_knowledge.md</code></li></ul>', 129)
  ])]);
}
const visualPrinciples = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  visualPrinciples as default
};
