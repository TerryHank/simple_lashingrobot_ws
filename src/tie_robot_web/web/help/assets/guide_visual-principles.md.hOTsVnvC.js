import { _ as _imports_0 } from "./chunks/pr-fprg-result.DYZDXurg.js";
import { _ as _export_sfc, o as openBlock, c as createElementBlock, ae as createStaticVNode } from "./chunks/framework.BxzTKQzU.js";
const __pageData = JSON.parse('{"title":"视觉原理","description":"","frontmatter":{},"headers":[],"relativePath":"guide/visual-principles.md","filePath":"guide/visual-principles.md"}');
const _sfc_main = { name: "guide/visual-principles.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="视觉原理" tabindex="-1">视觉原理 <a class="header-anchor" href="#视觉原理" aria-label="Permalink to &quot;视觉原理&quot;">​</a></h1><h2 id="当前认可方案" tabindex="-1">当前认可方案 <a class="header-anchor" href="#当前认可方案" aria-label="Permalink to &quot;当前认可方案&quot;">​</a></h2><p>当前前端按钮、扫描层和帮助站里提到的「当前认可 S2」，统一指：</p><ul><li>中文名：<code>PR-FPRG 透视展开频相回归网格方案</code></li><li>英文全称：<code>Projective-Rectified Frequency-Phase Regression Grid</code></li></ul><p>它不是在原始 IR 图像里直接铺满横竖线，也不再假设钢筋一定与画面水平/垂直。当前版本会先把手动工作区透视展开成规则平面，再在 <code>theta/rho</code> 空间估计两组钢筋真实摆放方向的周期线族。候选线必须经过一维峰值、二维连续钢筋条和主间距一致性检查，最后才把线交点投回原图。</p><p>完整逐步图像流程见：<a href="./pr-fprg-workflow">PR-FPRG 流程详解</a>。</p><h2 id="前端如何触发" tabindex="-1">前端如何触发 <a class="header-anchor" href="#前端如何触发" aria-label="Permalink to &quot;前端如何触发&quot;">​</a></h2><p>新前端的控制面板按钮显示为「触发 PR-FPRG」，内部仍复用历史动作 ID <code>runSavedS2</code>，这样可以少动已有 ROS 接口。</p><p>触发链如下：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>前端按钮 runSavedS2</span></span>\n<span class="line"><span>-&gt; TaskActionController.triggerSavedWorkspaceS2()</span></span>\n<span class="line"><span>-&gt; /web/pointAI/run_workspace_s2 std_msgs/Bool(data=true)</span></span>\n<span class="line"><span>-&gt; pointAI.manual_workspace_s2_callback()</span></span>\n<span class="line"><span>-&gt; run_manual_workspace_s2_pipeline(publish=true)</span></span>\n<span class="line"><span>-&gt; /pointAI/manual_workspace_s2_result_raw</span></span>\n<span class="line"><span>-&gt; /pointAI/manual_workspace_s2_points</span></span>\n<span class="line"><span>-&gt; /coordinate_point</span></span>\n<span class="line"><span>-&gt; /pointAI/result_image_raw</span></span>\n<span class="line"><span>-&gt; /tf pr_fprg_bind_point_*</span></span></code></pre></div><p>这些点话题表达相机坐标系下的视觉结果；前端 3D 显示层再通过 TF 把它们放到全局场景里。</p><p><code>process_image</code> 服务里的扫描、执行微调、绑扎检查等模式现在都以方向自适应 PR-FPRG 作为主视觉入口，不再要求旧 <code>pre_img()</code> 出点成功后才放行。</p><h2 id="当前方案效果图状态" tabindex="-1">当前方案效果图状态 <a class="header-anchor" href="#当前方案效果图状态" aria-label="Permalink to &quot;当前方案效果图状态&quot;">​</a></h2><p><img src="' + _imports_0 + '" alt="当前 PR-FPRG 结果"></p><p>当前帮助站效果图已经用新版探针从现场相机帧重新生成，来源是同步的 <code>raw_world_coord + world_coord + IR</code>，不是旧轴向截图。当前帧里钢筋本身接近 rectified 横/竖方向，所以画出来接近水平/垂直；如果钢筋在地面上斜摆，线族角度会随钢筋真实方向变化。</p><p>本次截图摘要：</p><ul><li>响应来源：<code>depth_background_minus_filled</code></li><li>线族角度：<code>88° / 178°</code></li><li>线族数量：<code>3 x 4</code></li><li>绑扎点数量：<code>12</code></li></ul><p>方向选择现在会先用响应图梯度做 <code>theta</code> 候选先验，再做 PR-FPRG 频相回归、峰值支撑、二维连续钢筋条验证和主间距一致性裁剪。底部原来容易出现的 13、14、15 号伪点应优先在二维连续钢筋条验证阶段删除；主间距一致性裁剪继续作为最后兜底。</p><h2 id="输入数据" tabindex="-1">输入数据 <a class="header-anchor" href="#输入数据" aria-label="Permalink to &quot;输入数据&quot;">​</a></h2><p>PR-FPRG 依赖 3 类输入：</p><ul><li>IR 图像：用于显示、叠加和人工检查。</li><li><code>/Scepter/worldCoord/raw_world_coord</code>：用于从像素交点反查原始相机系世界坐标。</li><li>手动工作区四边形：后端保存为 <code>corner_pixels</code>，用于确定透视展开范围。</li></ul><h2 id="算法主链" tabindex="-1">算法主链 <a class="header-anchor" href="#算法主链" aria-label="Permalink to &quot;算法主链&quot;">​</a></h2><ol><li>保存手动工作区四边形，并发布 <code>/pointAI/manual_workspace_quad_pixels</code> 给前端确认。</li><li>根据四边形构建正向透视矩阵 <code>forward_h</code> 和逆向透视矩阵 <code>inverse_h</code>。</li><li>从 <code>raw_world_coord[:, :, 2]</code> 取深度，透视展开并填补无效值。</li><li>用背景深度差生成钢筋凸起响应图。</li><li>从响应图梯度提取主方向先验，补齐正交方向候选。</li><li>扫描候选角度 <code>theta</code>，把二维响应图投影到法向 <code>rho</code>，得到每个方向的一维 profile。</li><li>对每个 profile 做频域周期检测与相位回归，选出两组近似正交且连续性更好的钢筋线族。</li><li>把候选 <code>rho</code> 线吸附到局部峰值，并删除峰值支撑不足的线。</li><li>在二维响应图上沿每条斜线采样，验证它是否是一整条连续钢筋条，并要求横截面呈现贴近候选线中心的 ridge。</li><li>按主网格间距兜底删除靠边或由垫高件造成的残留额外伪线。</li><li>两组线族用线方程求交点，通过 <code>inverse_h</code> 投回原图。</li><li>从 <code>raw_world_coord</code> 反查每个绑扎点的相机系三维坐标并发布。</li></ol><h2 id="代码落点" tabindex="-1">代码落点 <a class="header-anchor" href="#代码落点" aria-label="Permalink to &quot;代码落点&quot;">​</a></h2><p>前端触发代码：</p><ul><li><code>src/tie_robot_web/frontend/src/config/controlPanelCatalog.js</code>：控制面板按钮文案。</li><li><code>src/tie_robot_web/frontend/src/controllers/TaskActionController.js</code>：提交四边形、触发 PR-FPRG、固定扫描规划入口。</li><li><code>src/tie_robot_web/frontend/src/controllers/RosConnectionController.js</code>：发布 <code>/web/pointAI/run_workspace_s2</code>。</li><li><code>src/tie_robot_web/frontend/src/app/TieRobotFrontApp.js</code>：触发后切换覆盖层显示、等待 result 图。</li><li><code>src/tie_robot_web/frontend/src/views/WorkspaceCanvasView.js</code>：IR 图像选点、拖拽四边形、显示结果覆盖层。</li></ul><p>后端实现代码：</p><ul><li><code>src/tie_robot_perception/scripts/pointai_node.py</code>：pointAI ROS 节点可执行入口。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/node.py</code>：pointAI 节点实现，订阅 <code>/web/pointAI/run_workspace_s2</code>。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/workspace_masks.py</code>：保存与发布手动工作区四边形。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/manual_workspace_s2.py</code>：PR-FPRG 运行时主链。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/perception/workspace_s2.py</code>：方向线族估计、周期估计、峰值筛选、连续线验证、间距裁剪和线族求交。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/rendering.py</code>：结果图渲染与发布。</li><li><code>src/tie_robot_perception/src/tie_robot_perception/pointai/process_image_service.py</code>：<code>process_image</code> 主视觉入口，统一调用 PR-FPRG。</li><li><code>src/tie_robot_perception/tools/pr_fprg_peak_supported_probe.py</code>：独立探针，可导出逐步处理图片。</li></ul><h2 id="旧-ransac-hough-已归档" tabindex="-1">旧 RANSAC + Hough 已归档 <a class="header-anchor" href="#旧-ransac-hough-已归档" aria-label="Permalink to &quot;旧 RANSAC + Hough 已归档&quot;">​</a></h2><p>相机上游世界点处理在：</p><ul><li><code>src/tie_robot_perception/src/perception/scepter_world_coord_processor.cpp</code></li></ul><p>这里会把原始深度图转成三维点，再用 <code>SACMODEL_PLANE</code> 和 <code>SAC_RANSAC</code> 拟合主平面。系统同时保留两套世界点：</p><ul><li><code>raw_world_coord</code>：未经过主平面剔除的原始世界点。</li><li><code>world_coord</code>：去掉主平面后的残差世界点。</li></ul><p>PR-FPRG 更依赖 <code>raw_world_coord</code>，因为它需要从规则网格交点反查真实相机系坐标。</p><p>旧 <code>pre_img()</code> 绑扎点识别链路曾经负责深度二值化、细化、霍夫线提取、交点聚类和可执行范围过滤。现在这套 <code>RANSAC + Hough + pre_img</code> pointAI 链路已经从 active package 中移除，归档在：</p><ul><li><code>docs/archive/legacy_ransac_hough_pointai/</code></li></ul><p>当前分工是：</p><ol><li>相机上游继续提供 <code>raw_world_coord</code> 和 <code>world_coord</code>。</li><li>方向自适应 PR-FPRG 负责 pointAI 绑扎点主视觉识别。</li><li>旧 <code>pre_img()</code> 归档保留用于追溯，不参与 ROS 节点运行。</li></ol><p>注意：上游 <code>scepter_world_coord_processor.cpp</code> 中的 PCL <code>SAC_RANSAC</code> 平面处理属于世界坐标生成链路，不等同于已经归档的旧 pointAI RANSAC+Hough 绑扎点识别。</p><h2 id="不要退回的旧做法" tabindex="-1">不要退回的旧做法 <a class="header-anchor" href="#不要退回的旧做法" aria-label="Permalink to &quot;不要退回的旧做法&quot;">​</a></h2><p>以下做法都视为退化，不应再恢复：</p><ol><li>直接在工作区像素 bbox 里估周期。</li><li>在原图里直接铺满横竖线。</li><li>只凭一维 profile 峰值生成整张网格。</li><li>把方向自适应线族退回固定 X/Y 方向。</li><li>把 PR-FPRG 重新挂到旧 <code>pre_img()</code> 或 Hough 结果之后。</li><li>修改显示层时忘记把 rectified 线和点逆投影回原图。</li></ol>', 42)
  ])]);
}
const visualPrinciples = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  visualPrinciples as default
};
