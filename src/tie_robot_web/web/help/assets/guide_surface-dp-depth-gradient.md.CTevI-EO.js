import { _ as _export_sfc, o as openBlock, c as createElementBlock, ae as createStaticVNode } from "./chunks/framework.BxzTKQzU.js";
const _imports_0 = "/help/images/visual/surface-dp-depth-gradient/input-workspace.png";
const _imports_1 = "/help/images/visual/surface-dp-depth-gradient/rectified-ir.png";
const _imports_2 = "/help/images/visual/surface-dp-depth-gradient/rectified-depth.png";
const _imports_3 = "/help/images/visual/surface-dp-depth-gradient/depth-gradient-lines.png";
const _imports_4 = "/help/images/visual/surface-dp-depth-gradient/depth-gradient-original.png";
const _imports_5 = "/help/images/visual/surface-dp-depth-gradient/hidden-depth-response.png";
const _imports_6 = "/help/images/visual/surface-dp-depth-gradient/hidden-combined-response.png";
const _imports_7 = "/help/images/visual/surface-dp-depth-gradient/hidden-frangi-like.png";
const _imports_8 = "/help/images/visual/surface-dp-depth-gradient/hidden-hessian-ridge.png";
const _imports_9 = "/help/images/visual/surface-dp-depth-gradient/hidden-infrared-response.png";
const _imports_10 = "/help/images/visual/surface-dp-depth-gradient/hidden-fused-instance-response.png";
const _imports_11 = "/help/images/visual/surface-dp-depth-gradient/legacy-completed-surface.png";
const __pageData = JSON.parse('{"title":"Surface-DP 当前视觉方案","description":"","frontmatter":{},"headers":[],"relativePath":"guide/surface-dp-depth-gradient.md","filePath":"guide/surface-dp-depth-gradient.md"}');
const _sfc_main = { name: "guide/surface-dp-depth-gradient.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="surface-dp-当前视觉方案" tabindex="-1">Surface-DP 当前视觉方案 <a class="header-anchor" href="#surface-dp-当前视觉方案" aria-label="Permalink to &quot;Surface-DP 当前视觉方案&quot;">​</a></h1><p>扫描层视觉现在采用<strong>设置页选择的一种底图</strong>作为主链响应。每次运行只生成被激活的底图，并在这张底图上完成物理线族识别、统一物理网格校对和 Surface-DP 曲线追踪；其它底图保留在代码和离线工具中，只作为解释、对照和回溯材料。</p><h2 id="当前口径" tabindex="-1">当前口径 <a class="header-anchor" href="#当前口径" aria-label="Permalink to &quot;当前口径&quot;">​</a></h2><ul><li>中文名：Surface-DP 单源底图方案。</li><li>默认运行源：<code>depth_gradient</code>（深度梯度边缘）。</li><li>运行策略：<code>single_selected_response</code>。</li><li>运行次数：只做一次响应图构建和一次物理线族识别。</li><li>物理线距先验：120-160 mm。</li><li>网格校对：<code>unified_physical_lattice</code>，不按小视野、大视野、长方形或正方形分档。</li><li>可选底图：融合实例响应、Frangi-like 脊线、Hessian ridge 脊线、深度梯度边缘、红外响应、组合响应、深度响应。</li></ul><p>这版口径的含义是：主链只相信当前选中的一张底图，不再先跑一轮多模态候选、再跑一轮旧补全面候选。默认的深度梯度边缘更关注深度图上的局部变化边缘；如果现场光照、反光或钢筋形态变化，可以在前端「设置 / 视觉调试」里的「扫描底图」下拉栏切换其它底图。前端会把上一次选择保存到本地，并在 ROS 重连后自动下发给 PointAI。</p><h2 id="主链流程" tabindex="-1">主链流程 <a class="header-anchor" href="#主链流程" aria-label="Permalink to &quot;主链流程&quot;">​</a></h2><ol><li>读取手动工作区四边形和当前 <code>raw_world_coord</code> / 深度数据。</li><li>将工作区透视展开成 rectified 画幅。</li><li>根据「扫描底图」选择，只生成当前被激活的一张响应图。</li><li>对响应图做阈值和骨架诊断；这些诊断不直接产出绑扎点。</li><li>在当前响应图上按 120-160 mm 物理钢筋间距寻找横纵线族。</li><li>用统一物理网格评分校对线族，拒绝线数比例和可见物理视野长宽不一致的结果。</li><li>用 Surface-DP 沿当前响应图做曲线追踪，得到两组曲线线族。</li><li>求曲线交点，通过 inverse H 投回原图，再从 <code>raw_world_coord</code> 反查相机坐标。</li><li>发布扫描点、结果图、扫描底图诊断图和 <code>surface_dp_bind_point_*</code> TF。</li></ol><p>梁筋候选仍作为当前运行链路的一层诊断和可选过滤：<code>beam_candidate_bands</code> 会在扫描底图上显示为红色竖带；前端启用梁筋过滤后，PointAI 按设置半径扩张候选 mask，只删除落入 mask 的最终交点。它不会因为一个交点命中梁筋 mask，就连带移除同一 X 位置的其它交点。</p><p>默认 <code>depth_gradient</code> 的构建步骤是：对填补后的深度图做轻量高斯平滑，用 Sobel 计算 X / Y 方向梯度，再取梯度幅值并归一化。</p><h2 id="前端切换" tabindex="-1">前端切换 <a class="header-anchor" href="#前端切换" aria-label="Permalink to &quot;前端切换&quot;">​</a></h2><ul><li>入口：<code>设置 / 视觉调试 / 扫描底图</code>。</li><li>前端话题：<code>/web/pointAI/set_scan_response_source</code>。</li><li>消息类型：<code>std_msgs/String</code>。</li><li>后端参数：<code>~scan_response_source</code>。</li><li>默认值：<code>depth_gradient</code>。</li></ul><p>可选值如下：</p><table tabindex="0"><thead><tr><th>中文名</th><th>参数值</th></tr></thead><tbody><tr><td>融合实例响应</td><td><code>fused_instance_response</code></td></tr><tr><td>Frangi-like 脊线</td><td><code>frangi_like</code></td></tr><tr><td>Hessian ridge 脊线</td><td><code>hessian_ridge</code></td></tr><tr><td>深度梯度边缘</td><td><code>depth_gradient</code></td></tr><tr><td>红外响应</td><td><code>infrared_response</code></td></tr><tr><td>组合响应</td><td><code>combined_response</code></td></tr><tr><td>深度响应</td><td><code>depth_response</code></td></tr></tbody></table><h2 id="主链效果图" tabindex="-1">主链效果图 <a class="header-anchor" href="#主链效果图" aria-label="Permalink to &quot;主链效果图&quot;">​</a></h2><p>每个流程节点的完整效果图见：<a href="./current-visual-flow">当前视觉流程效果图</a>。这里保留 Surface-DP 主链的核心图。</p><p>当前输入工作区：</p><p><img src="' + _imports_0 + '" alt="当前输入工作区"></p><p>透视展开后的红外参考图：</p><p><img src="' + _imports_1 + '" alt="透视展开红外参考"></p><p>透视展开后的深度参考图：</p><p><img src="' + _imports_2 + '" alt="透视展开深度参考"></p><p>深度梯度边缘上的横纵线族：</p><p><img src="' + _imports_3 + '" alt="深度梯度边缘线族"></p><p>深度梯度结果投回原图：</p><p><img src="' + _imports_4 + '" alt="深度梯度原图投影"></p><h2 id="统一物理网格校对" tabindex="-1">统一物理网格校对 <a class="header-anchor" href="#统一物理网格校对" aria-label="Permalink to &quot;统一物理网格校对&quot;">​</a></h2><p>旧版曾经用「横纵线族数量接近 <code>1:1</code>」来拒绝极端假阳，这对正方形网格有用，但会误伤长方形钢筋面。当前版本改为统一物理网格评分：</p><ul><li>横纵线数可以不相等，允许 <code>3 m x 5 m</code> 这类长方形版面自然出现更多长边方向钢筋。</li><li>校对使用线数间隔比例，而不是线条数量本身。</li><li>线数间隔比例要匹配 rectified 有效 mask 的物理长宽比。</li><li>小视野因为可见间隔少，会自动获得更宽的 tolerance，但仍要求两轴都成立。</li><li><code>16 x 2</code> 这类只在一个方向大量出线、另一个方向近乎缺失的结果，如果和视野物理长宽不匹配，会被 <code>count_aspect_mismatch</code> 拒绝。</li></ul><p>诊断字段：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>physical_prior_modes = [unified_physical_lattice, unified_physical_lattice]</span></span>\n<span class="line"><span>physical_lattice_score</span></span>\n<span class="line"><span>physical_lattice_count_aspect</span></span>\n<span class="line"><span>physical_lattice_visible_aspect</span></span>\n<span class="line"><span>physical_lattice_count_aspect_error</span></span>\n<span class="line"><span>physical_lattice_count_aspect_tolerance</span></span></code></pre></div><p>判断规则：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>physical_lattice_count_aspect_error &lt;= physical_lattice_count_aspect_tolerance</span></span></code></pre></div><p>成立时，候选网格的线数比例和当前可见物理视野一致；不成立时，说明线族更像局部杂线、梁筋、边缘反光或单轴假阳。</p><h2 id="隐藏模态说明" tabindex="-1">隐藏模态说明 <a class="header-anchor" href="#隐藏模态说明" aria-label="Permalink to &quot;隐藏模态说明&quot;">​</a></h2><p>未选中的模态不进入扫描层运行主链，但保留在帮助文档和离线诊断工具中，便于对比哪一种底图更适合现场画面。</p><table tabindex="0"><thead><tr><th>底图</th><th>原理</th><th>在当前口径中的位置</th></tr></thead><tbody><tr><td>深度响应</td><td>用局部背景深度减去当前深度，突出高于背景的结构。</td><td>可选主链底图。</td></tr><tr><td>红外响应</td><td>用局部红外背景减去当前红外强度，突出红外变暗的线状区域。</td><td>可选主链底图。</td></tr><tr><td>组合响应</td><td>将深度响应和红外响应按权重融合。</td><td>可选主链底图。</td></tr><tr><td>Hessian ridge</td><td>在组合响应上用二阶导数提取亮脊线。</td><td>可选主链底图。</td></tr><tr><td>Frangi-like</td><td>多尺度提取线状脊结构，适合连续细线。</td><td>可选主链底图。</td></tr><tr><td>融合实例响应</td><td>混合组合响应、深度、Frangi、梯度和 Hessian。</td><td>可选主链底图。</td></tr><tr><td>旧补全钢筋面响应</td><td>第一轮找线后把线族画回响应图，再做第二轮找线。</td><td>仅离线对照；主链不再使用第二轮。</td></tr></tbody></table><p>深度暗线响应离线效果：</p><p><img src="' + _imports_5 + '" alt="深度暗线响应离线效果"></p><p>深度 + 红外组合响应离线效果：</p><p><img src="' + _imports_6 + '" alt="深度 + 红外组合响应离线效果"></p><p>Frangi-like 离线效果：</p><p><img src="' + _imports_7 + '" alt="Frangi-like 离线效果"></p><p>Hessian ridge 离线效果：</p><p><img src="' + _imports_8 + '" alt="Hessian ridge 离线效果"></p><p>红外响应离线效果：</p><p><img src="' + _imports_9 + '" alt="红外响应离线效果"></p><p>融合实例响应离线效果：</p><p><img src="' + _imports_10 + '" alt="融合实例响应离线效果"></p><p>旧补全钢筋面离线效果：</p><p><img src="' + _imports_11 + '" alt="旧补全钢筋面离线效果"></p><h2 id="运行诊断字段" tabindex="-1">运行诊断字段 <a class="header-anchor" href="#运行诊断字段" aria-label="Permalink to &quot;运行诊断字段&quot;">​</a></h2><p><code>build_scan_surface_dp_result()</code> 的诊断信息会给出当前选择：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>scan_runtime_response_policy = single_selected_response</span></span>\n<span class="line"><span>scan_runtime_response_source = depth_gradient       # 或当前下拉栏选择的其它源</span></span>\n<span class="line"><span>scan_runtime_response_source_requested = depth_gradient</span></span>\n<span class="line"><span>base_physical_source = depth_gradient               # 与当前选中源一致</span></span>\n<span class="line"><span>completed_physical_source = depth_gradient          # 兼容字段，与当前选中源一致</span></span>\n<span class="line"><span>beam_candidate_count</span></span>\n<span class="line"><span>beam_candidate_margin_pixels</span></span>\n<span class="line"><span>beam_filter_mode = point_mask                       # 启用梁筋过滤时只做点级删除</span></span>\n<span class="line"><span>beam_filtered_point_count</span></span></code></pre></div><p>这里保留 <code>base_physical_source</code> 和 <code>completed_physical_source</code> 字段，是为了兼容已有日志和前端显示；在当前口径下，两者都应指向当前选中的同一个源。实际运行不再有第一轮多模态候选和第二轮补全面候选。</p><h2 id="相机-sdk-热修改" tabindex="-1">相机 SDK 热修改 <a class="header-anchor" href="#相机-sdk-热修改" aria-label="Permalink to &quot;相机 SDK 热修改&quot;">​</a></h2><p>「设置 / 相机底层 SDK 调试」面板用来热修改 <code>scepter_manager</code> 的 <code>dynamic_reconfigure</code> 参数。它走的是 ROS 官方动态参数方案，等价于手工运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rosrun</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rqt_reconfigure</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rqt_reconfigure</span></span></code></pre></div><p>前端调用的服务是 <code>/scepter_manager/set_parameters</code>，服务类型是 <code>dynamic_reconfigure/Reconfigure</code>；同时监听 <code>/scepter_manager/parameter_updates</code> 和 <code>/scepter_manager/parameter_descriptions</code>。参数名沿用相机驱动的 <code>Sceptertof_roscpp.cfg</code>，例如 <code>FrameRate</code>、<code>XDRMode</code>、<code>ToFManual</code>、<code>ToFExposureTime</code>、<code>ColorResloution</code>、<code>DepthCloudPoint</code> 等。前端会把面板里的值保存到浏览器本地设置，ROS 重连后自动回放，避免每次重启都要重新手动调一遍。</p>', 58)
  ])]);
}
const surfaceDpDepthGradient = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  surfaceDpDepthGradient as default
};
